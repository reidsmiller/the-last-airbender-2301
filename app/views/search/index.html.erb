<h1>Members of the <%= @facade.nation.capitalize %></h1>

<div class='total_members'>
  <p>Total Number of People: <%= @facade.member_count %></p>
</div>

<div class='member_data'>
  <ol>
    <% @facade.members.each do |member| %>
      <li class='member'>
        <ul>
          <h4>Name: <%= member.name %></h4>
          <% if member.photo_url %>
            <p class='char_img'><%= image_tag(member.photo_url) %></p>
          <% end %>
          <p>Allies: <%= member.allies_list %></p>
          <p>Enemies: <%= member.enemies_list %></p>
          <p>Affiliation: <%= member.affiliation %></p>
        </ul>
      </li>
    <% end %>
  </ol>
</div>